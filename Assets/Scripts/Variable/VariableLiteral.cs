using UnityEngine;

// 데이터 값 (Literal) - 변수에 저장할 데이터 값의 종료
public class VariableLiteral : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int num;
        double su;
    }

    
}
